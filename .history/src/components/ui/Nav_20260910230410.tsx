
export interface NavLink{
    anchor:string,
    name:te
}

export interface NavProps{
    
}

const Nav = ({
    
}:NavProps) => {

    return(
        <nav>
        </nav>
    )
}

export default Nav;